# Day 1 Summary

1. Created and executed a local Python script successfully.

2. Logged into the cluster via SSH and ran scripts (`xyd.py` and `xyd_screen.py`) for testing.

3. Forked a repository, created a branch `xyd`, added `xyd.md` in the `2026` directory, committed and pushed the changes, and created a pull request.

4. Opened Jupyter Notebook, ran a notebook, and became familiar with basic operations.